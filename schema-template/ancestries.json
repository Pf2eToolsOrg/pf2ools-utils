{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"version": "0.0.1",
	"type": "object",
	"$id": "schema.json",
	"title": "Schema",
	"description": "Schema for PF2ools. Should include arrays titled similarly to the main site data, e.g. `spell` or `class`. Requires `_meta` if made as homebrew to function.",
	"properties": {
		"ancestry": {
			"$ref": "ancestries.json#/properties/ancestry"
		}
	},
	"additionalProperties": false,
	"minProperties": 1
}